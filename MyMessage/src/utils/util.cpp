#include "util.h"


void Util::SplitStr(const QString &s, QStringList &v, const QString &c)
{
    if (s.isEmpty() || c.isEmpty())
        return;

    for (auto ca : s)
    {
        if (ca == c)
        {

        }
    }
}
